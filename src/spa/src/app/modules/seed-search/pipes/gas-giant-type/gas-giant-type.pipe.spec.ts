import { GasGiantTypePipe } from './gas-giant-type.pipe';

describe('GasGiantTypePipe', () => {
  it('create an instance', () => {
    const pipe = new GasGiantTypePipe();
    expect(pipe).toBeTruthy();
  });
});
